# Next.js build output (if accidentally run from repo root)
.next/

# Claude Code local settings
.claude/settings.local.json

# Design bundle extraction artifacts
todoist__*
_design_*

# Local planning docs
VOICE_AGENT_PLAN.md
